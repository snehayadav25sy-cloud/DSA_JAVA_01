class Solution {
    public int[] dailyTemperatures(int[] temperatures) {
        int n = temperatures.length;
        int[] warmer = new int[n];
        
        Stack<Integer> st = new Stack<>();
        int nge = 0;

        for(int i = 0 ; i<n ; i++){
            while(!st.isEmpty() && temperatures[st.peek()]<temperatures[i]){
                int element = st.pop();
                warmer[element] = i - element ;          
            }

            st.push(i);
        }

        return warmer ;
    }
}
